export default {
    PRODUCTS(state) {
      return state.products;
    },
    CART(state) {
      return state.cart;
    },
    CATEGORIES(state) {
      return state.categories;
    },
    CATEGORY(state) {
      return state.checkedCategory;
    }
  }
